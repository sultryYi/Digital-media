值变为0.2
            # self.img['descriptor'][i][self.img['descriptor'][i] > 0.2] = 0.2
            # # 再做一次归一化
            # vector128 = self.img['descriptor'][i].copy()
            # self.img['descriptor'][i